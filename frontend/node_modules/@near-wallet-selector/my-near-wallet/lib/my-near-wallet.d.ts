import type { WalletModuleFactory, BrowserWallet } from "@near-wallet-selector/core";
export interface MyNearWalletParams {
    walletUrl?: string;
    iconUrl?: string;
}
export declare function setupMyNearWallet({ walletUrl, iconUrl, }?: MyNearWalletParams): WalletModuleFactory<BrowserWallet>;
