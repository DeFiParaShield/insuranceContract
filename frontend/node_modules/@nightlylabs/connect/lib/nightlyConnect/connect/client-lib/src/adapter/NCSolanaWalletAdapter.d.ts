import { PublicKey, Transaction } from '@solana/web3.js';
import { NightlyConnectModal } from '../modal/NightlyConnectModal';
import { BaseSignerWalletAdapter, WalletName, WalletReadyState } from '@solana/wallet-adapter-base';
import { SolanaAppInfo } from '../../../sdk/src/types/AppInfo';
export declare const NightlyConnectWalletName: WalletName;
export declare class NCSolanaWalletAdapter extends BaseSignerWalletAdapter {
    name: WalletName;
    url: string;
    icon: string;
    private _connecting;
    private _connected;
    private _publicKey;
    private _readyState;
    private _app;
    modal: NightlyConnectModal;
    private _appInfo;
    constructor(appInfo: Omit<SolanaAppInfo, 'onUserConnect'>);
    get connected(): boolean;
    get connecting(): boolean;
    get readyState(): WalletReadyState;
    get publicKey(): PublicKey | null;
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    signTransaction(transaction: Transaction): Promise<Transaction>;
    signAllTransactions(transactions: Transaction[]): Promise<Transaction[]>;
}
